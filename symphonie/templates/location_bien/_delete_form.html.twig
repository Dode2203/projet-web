<form method="post" action="{{ path('app_location_bien_delete', {'idLocationBien': location_bien.idLocationBien}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ location_bien.idLocationBien) }}">
    <button class="btn">Delete</button>
</form>
