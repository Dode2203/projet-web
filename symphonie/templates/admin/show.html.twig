{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdAdmin</th>
                <td>{{ admin.idAdmin }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ admin.login }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ admin.mdp }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ admin.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_index') }}">back to list</a>

    <a href="{{ path('app_admin_edit', {'idAdmin': admin.idAdmin}) }}">edit</a>

    {{ include('admin/_delete_form.html.twig') }}
{% endblock %}
