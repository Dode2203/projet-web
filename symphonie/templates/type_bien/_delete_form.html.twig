<form method="post" action="{{ path('app_type_bien_delete', {'idTypeBien': type_bien.idTypeBien}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_bien.idTypeBien) }}">
    <button class="btn">Delete</button>
</form>
