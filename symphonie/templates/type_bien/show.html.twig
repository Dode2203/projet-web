{% extends 'base.html.twig' %}

{% block title %}TypeBien{% endblock %}

{% block body %}
    <h1>TypeBien</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdTypeBien</th>
                <td>{{ type_bien.idTypeBien }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ type_bien.nom }}</td>
            </tr>
            <tr>
                <th>Comition</th>
                <td>{{ type_bien.comition }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_bien_index') }}">back to list</a>

    <a href="{{ path('app_type_bien_edit', {'idTypeBien': type_bien.idTypeBien}) }}">edit</a>

    {{ include('type_bien/_delete_form.html.twig') }}
{% endblock %}
