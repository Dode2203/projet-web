<form method="post" action="{{ path('app_bien_delete', {'idBien': bien.idBien}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bien.idBien) }}">
    <button class="btn">Delete</button>
</form>
