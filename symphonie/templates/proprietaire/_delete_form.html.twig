<form method="post" action="{{ path('app_proprietaire_delete', {'idProprietaire': proprietaire.idProprietaire}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proprietaire.idProprietaire) }}">
    <button class="btn">Delete</button>
</form>
